baseURL = 'https://vanilla.sh/'
languageCode = 'en-GB'
title = 'Vanilla.sh'
theme = 'vanillash'

disableKinds = ["taxonomy", "term"]

[markup]
    [markup.goldmark]
        [markup.goldmark.renderer]
            unsafe = true